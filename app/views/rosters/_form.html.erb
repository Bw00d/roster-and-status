<%= form_with(model: roster, local: true) do |form| %>
  <% if roster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roster.errors.count, "error") %> prohibited this roster from being saved:</h2>

      <ul>
      <% roster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lead %>
    <%= form.number_field :lead, id: :roster_lead %>
  </div>

  <div class="field">
    <%= form.label :assistant %>
    <%= form.number_field :assistant, id: :roster_assistant %>
  </div>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_select :start_date, id: :roster_start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date, id: :roster_end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
